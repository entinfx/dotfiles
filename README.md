# Various dotfiles for my terminal and dev tools


## Bash setup
1. As configured in '~/.bashrc', Bash will attempt to run 'neofetch' on launch.
   Make sure Neofetch is installed: `$ sudo apt install neofetch` (on Ubuntu).

